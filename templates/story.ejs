import React from 'react'
import <%= props.name %> from './<%= props.name %>'
import { storiesOf } from '@storybook/react'

storiesOf('<%= props.name %>')
  .add('Initial story', () => (
    <<%= props.name %> />
  ))
